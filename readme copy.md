# File: README.md
# ai-admin-agent (scaffold)
Lightweight scaffold for an AI-powered system administrator assistant.
This repo provides a safe, modular starting point.

## Quickstart (local)
1. Create virtual env and install:
   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
